# Código legacy (no compilado)

Adaptadores y opciones de fases anteriores (`SqlTest`, `SapPostgres`, SQL Server como proveedor EF).

No están registrados en DI ni forman parte del camino operativo HANA. Se conservan solo como referencia histórica.

Modo de ingesta activo: `Source:Mode=SapHana` → `SapHanaJobSourceAdapter`.
